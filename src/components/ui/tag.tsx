export function Tag({ children }: { children: React.ReactNode }) {
  return (
    <span className="inline-block px-2 py-0.5 text-xs border border-surface1 text-subtext0 rounded-sm">
      {children}
    </span>
  );
}
